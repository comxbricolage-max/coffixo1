# MongoDB Connection
# Replace <db_password> with your actual MongoDB Atlas password
MONGODB_URI=mongodb+srv://comx2025:<db_password>@caffixo.m1zvqsz.mongodb.net/?appName=caffixo

# Optional: Database Name (defaults to 'caffexo')
# MONGODB_DB_NAME=caffexo

# Optional: Node Environment
# NODE_ENV=development

# Note: In demo mode, MONGODB_URI is optional
# The application works without a database connection for testing
